{
  "name": "copasave-backend",
  "version": "1.0.0",
  "description": "Copasave video downloader API — Express + yt-dlp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "pip install yt-dlp --break-system-packages || pip3 install yt-dlp --break-system-packages"
},
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
